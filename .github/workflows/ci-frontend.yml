name: CI Frontend

# Placeholder — will be activated in Phase 6 (Web UI)

on:
  push:
    paths:
      - 'frontend/**'
  pull_request:
    paths:
      - 'frontend/**'

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Frontend CI not yet configured
        run: echo "Frontend CI will be configured in Phase 6"
